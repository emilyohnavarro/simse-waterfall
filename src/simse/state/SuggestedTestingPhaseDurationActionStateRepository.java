/* File generated by: simse.codegenerator.stategenerator.RepositoryGenerator */
package simse.state;

import simse.adts.objects.*;
import simse.adts.actions.*;
import java.util.*;

public class SuggestedTestingPhaseDurationActionStateRepository implements
		Cloneable {
	private Vector<SuggestedTestingPhaseDurationAction> actions;

	public SuggestedTestingPhaseDurationActionStateRepository() {
		actions = new Vector<SuggestedTestingPhaseDurationAction>();
	}

	public Object clone() {
		try {
			SuggestedTestingPhaseDurationActionStateRepository cl = (SuggestedTestingPhaseDurationActionStateRepository) (super
					.clone());
			Vector<SuggestedTestingPhaseDurationAction> clonedActions = new Vector<SuggestedTestingPhaseDurationAction>();
			for (int i = 0; i < actions.size(); i++) {
				clonedActions.add((SuggestedTestingPhaseDurationAction) actions
						.elementAt(i).clone());
			}
			cl.actions = clonedActions;
			return cl;
		} catch (CloneNotSupportedException c) {
			System.out.println(c.getMessage());
		}
		return null;
	}

	public boolean add(SuggestedTestingPhaseDurationAction a) {
		if (actions.contains(a) == false) {
			actions.add(a);
			return true;
		}
		return false;
	}

	public boolean remove(SuggestedTestingPhaseDurationAction a) {
		if (actions.contains(a)) {
			actions.remove(a);
			return true;
		}
		return false;
	}

	public Vector<SuggestedTestingPhaseDurationAction> getAllActions() {
		return actions;
	}

	public Vector<SuggestedTestingPhaseDurationAction> getAllActions(SSObject a) {
		Vector<SuggestedTestingPhaseDurationAction> all = new Vector<SuggestedTestingPhaseDurationAction>();
		for (int i = 0; i < actions.size(); i++) {
			SuggestedTestingPhaseDurationAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public Vector<SuggestedTestingPhaseDurationAction> getAllActiveActions(
			SSObject a) {
		Vector<SuggestedTestingPhaseDurationAction> all = new Vector<SuggestedTestingPhaseDurationAction>();
		for (int i = 0; i < actions.size(); i++) {
			SuggestedTestingPhaseDurationAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllActiveParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public Vector<SuggestedTestingPhaseDurationAction> getAllInactiveActions(
			SSObject a) {
		Vector<SuggestedTestingPhaseDurationAction> all = new Vector<SuggestedTestingPhaseDurationAction>();
		for (int i = 0; i < actions.size(); i++) {
			SuggestedTestingPhaseDurationAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllInactiveParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public SuggestedTestingPhaseDurationAction getActionWithId(int id) {
		for (int i = 0; i < actions.size(); i++) {
			SuggestedTestingPhaseDurationAction act = actions.get(i);
			if (act.getId() == id) {
				return act;
			}
		}
		return null;
	}

	/*
	 * Replaces all the participants in each action with their equivalent
	 * objects in the current state. Calling this function solves the problem
	 * that happens when you clone actions -- their hashtables point to
	 * participant objects that were part of the previous, non-cloned state.
	 * Hence, this function should be called after this object is cloned.
	 */
	public void refetchParticipants(ArtifactStateRepository artifactRep,
			CustomerStateRepository customerRep,
			EmployeeStateRepository employeeRep,
			ProjectStateRepository projectRep, ToolStateRepository toolRep) {
		for (int i = 0; i < actions.size(); i++) {
			SuggestedTestingPhaseDurationAction act = actions.elementAt(i);
			act.refetchParticipants(artifactRep, customerRep, employeeRep,
					projectRep, toolRep);
		}
	}
}