/* File generated by: simse.codegenerator.stategenerator.RepositoryGenerator */
package simse.state;

import simse.adts.objects.*;
import simse.adts.actions.*;
import java.util.*;

public class PurchaseToolActionStateRepository implements Cloneable {
	private Vector<PurchaseToolAction> actions;

	public PurchaseToolActionStateRepository() {
		actions = new Vector<PurchaseToolAction>();
	}

	public Object clone() {
		try {
			PurchaseToolActionStateRepository cl = (PurchaseToolActionStateRepository) (super
					.clone());
			Vector<PurchaseToolAction> clonedActions = new Vector<PurchaseToolAction>();
			for (int i = 0; i < actions.size(); i++) {
				clonedActions.add((PurchaseToolAction) actions.elementAt(i)
						.clone());
			}
			cl.actions = clonedActions;
			return cl;
		} catch (CloneNotSupportedException c) {
			System.out.println(c.getMessage());
		}
		return null;
	}

	public boolean add(PurchaseToolAction a) {
		if (actions.contains(a) == false) {
			actions.add(a);
			return true;
		}
		return false;
	}

	public boolean remove(PurchaseToolAction a) {
		if (actions.contains(a)) {
			actions.remove(a);
			return true;
		}
		return false;
	}

	public Vector<PurchaseToolAction> getAllActions() {
		return actions;
	}

	public Vector<PurchaseToolAction> getAllActions(SSObject a) {
		Vector<PurchaseToolAction> all = new Vector<PurchaseToolAction>();
		for (int i = 0; i < actions.size(); i++) {
			PurchaseToolAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public Vector<PurchaseToolAction> getAllActiveActions(SSObject a) {
		Vector<PurchaseToolAction> all = new Vector<PurchaseToolAction>();
		for (int i = 0; i < actions.size(); i++) {
			PurchaseToolAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllActiveParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public Vector<PurchaseToolAction> getAllInactiveActions(SSObject a) {
		Vector<PurchaseToolAction> all = new Vector<PurchaseToolAction>();
		for (int i = 0; i < actions.size(); i++) {
			PurchaseToolAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllInactiveParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public PurchaseToolAction getActionWithId(int id) {
		for (int i = 0; i < actions.size(); i++) {
			PurchaseToolAction act = actions.get(i);
			if (act.getId() == id) {
				return act;
			}
		}
		return null;
	}

	/*
	 * Replaces all the participants in each action with their equivalent
	 * objects in the current state. Calling this function solves the problem
	 * that happens when you clone actions -- their hashtables point to
	 * participant objects that were part of the previous, non-cloned state.
	 * Hence, this function should be called after this object is cloned.
	 */
	public void refetchParticipants(ArtifactStateRepository artifactRep,
			CustomerStateRepository customerRep,
			EmployeeStateRepository employeeRep,
			ProjectStateRepository projectRep, ToolStateRepository toolRep) {
		for (int i = 0; i < actions.size(); i++) {
			PurchaseToolAction act = actions.elementAt(i);
			act.refetchParticipants(artifactRep, customerRep, employeeRep,
					projectRep, toolRep);
		}
	}
}