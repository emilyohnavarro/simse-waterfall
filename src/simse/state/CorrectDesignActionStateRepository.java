/* File generated by: simse.codegenerator.stategenerator.RepositoryGenerator */
package simse.state;

import simse.adts.objects.*;
import simse.adts.actions.*;
import java.util.*;

public class CorrectDesignActionStateRepository implements Cloneable {
	private Vector<CorrectDesignAction> actions;

	public CorrectDesignActionStateRepository() {
		actions = new Vector<CorrectDesignAction>();
	}

	public Object clone() {
		try {
			CorrectDesignActionStateRepository cl = (CorrectDesignActionStateRepository) (super
					.clone());
			Vector<CorrectDesignAction> clonedActions = new Vector<CorrectDesignAction>();
			for (int i = 0; i < actions.size(); i++) {
				clonedActions.add((CorrectDesignAction) actions.elementAt(i)
						.clone());
			}
			cl.actions = clonedActions;
			return cl;
		} catch (CloneNotSupportedException c) {
			System.out.println(c.getMessage());
		}
		return null;
	}

	public boolean add(CorrectDesignAction a) {
		if (actions.contains(a) == false) {
			actions.add(a);
			return true;
		}
		return false;
	}

	public boolean remove(CorrectDesignAction a) {
		if (actions.contains(a)) {
			actions.remove(a);
			return true;
		}
		return false;
	}

	public Vector<CorrectDesignAction> getAllActions() {
		return actions;
	}

	public Vector<CorrectDesignAction> getAllActions(SSObject a) {
		Vector<CorrectDesignAction> all = new Vector<CorrectDesignAction>();
		for (int i = 0; i < actions.size(); i++) {
			CorrectDesignAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public Vector<CorrectDesignAction> getAllActiveActions(SSObject a) {
		Vector<CorrectDesignAction> all = new Vector<CorrectDesignAction>();
		for (int i = 0; i < actions.size(); i++) {
			CorrectDesignAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllActiveParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public Vector<CorrectDesignAction> getAllInactiveActions(SSObject a) {
		Vector<CorrectDesignAction> all = new Vector<CorrectDesignAction>();
		for (int i = 0; i < actions.size(); i++) {
			CorrectDesignAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllInactiveParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public CorrectDesignAction getActionWithId(int id) {
		for (int i = 0; i < actions.size(); i++) {
			CorrectDesignAction act = actions.get(i);
			if (act.getId() == id) {
				return act;
			}
		}
		return null;
	}

	/*
	 * Replaces all the participants in each action with their equivalent
	 * objects in the current state. Calling this function solves the problem
	 * that happens when you clone actions -- their hashtables point to
	 * participant objects that were part of the previous, non-cloned state.
	 * Hence, this function should be called after this object is cloned.
	 */
	public void refetchParticipants(ArtifactStateRepository artifactRep,
			CustomerStateRepository customerRep,
			EmployeeStateRepository employeeRep,
			ProjectStateRepository projectRep, ToolStateRepository toolRep) {
		for (int i = 0; i < actions.size(); i++) {
			CorrectDesignAction act = actions.elementAt(i);
			act.refetchParticipants(artifactRep, customerRep, employeeRep,
					projectRep, toolRep);
		}
	}
}