/* File generated by: simse.codegenerator.stategenerator.RepositoryGenerator */
package simse.state;

import simse.adts.objects.*;
import simse.adts.actions.*;
import java.util.*;

public class QuitActionStateRepository implements Cloneable {
	private Vector<QuitAction> actions;

	public QuitActionStateRepository() {
		actions = new Vector<QuitAction>();
	}

	public Object clone() {
		try {
			QuitActionStateRepository cl = (QuitActionStateRepository) (super
					.clone());
			Vector<QuitAction> clonedActions = new Vector<QuitAction>();
			for (int i = 0; i < actions.size(); i++) {
				clonedActions.add((QuitAction) actions.elementAt(i).clone());
			}
			cl.actions = clonedActions;
			return cl;
		} catch (CloneNotSupportedException c) {
			System.out.println(c.getMessage());
		}
		return null;
	}

	public boolean add(QuitAction a) {
		if (actions.contains(a) == false) {
			actions.add(a);
			return true;
		}
		return false;
	}

	public boolean remove(QuitAction a) {
		if (actions.contains(a)) {
			actions.remove(a);
			return true;
		}
		return false;
	}

	public Vector<QuitAction> getAllActions() {
		return actions;
	}

	public Vector<QuitAction> getAllActions(SSObject a) {
		Vector<QuitAction> all = new Vector<QuitAction>();
		for (int i = 0; i < actions.size(); i++) {
			QuitAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public Vector<QuitAction> getAllActiveActions(SSObject a) {
		Vector<QuitAction> all = new Vector<QuitAction>();
		for (int i = 0; i < actions.size(); i++) {
			QuitAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllActiveParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public Vector<QuitAction> getAllInactiveActions(SSObject a) {
		Vector<QuitAction> all = new Vector<QuitAction>();
		for (int i = 0; i < actions.size(); i++) {
			QuitAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllInactiveParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public QuitAction getActionWithId(int id) {
		for (int i = 0; i < actions.size(); i++) {
			QuitAction act = actions.get(i);
			if (act.getId() == id) {
				return act;
			}
		}
		return null;
	}

	/*
	 * Replaces all the participants in each action with their equivalent
	 * objects in the current state. Calling this function solves the problem
	 * that happens when you clone actions -- their hashtables point to
	 * participant objects that were part of the previous, non-cloned state.
	 * Hence, this function should be called after this object is cloned.
	 */
	public void refetchParticipants(ArtifactStateRepository artifactRep,
			CustomerStateRepository customerRep,
			EmployeeStateRepository employeeRep,
			ProjectStateRepository projectRep, ToolStateRepository toolRep) {
		for (int i = 0; i < actions.size(); i++) {
			QuitAction act = actions.elementAt(i);
			act.refetchParticipants(artifactRep, customerRep, employeeRep,
					projectRep, toolRep);
		}
	}
}