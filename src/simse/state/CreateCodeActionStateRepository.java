/* File generated by: simse.codegenerator.stategenerator.RepositoryGenerator */
package simse.state;

import simse.adts.objects.*;
import simse.adts.actions.*;
import java.util.*;

public class CreateCodeActionStateRepository implements Cloneable {
	private Vector<CreateCodeAction> actions;

	public CreateCodeActionStateRepository() {
		actions = new Vector<CreateCodeAction>();
	}

	public Object clone() {
		try {
			CreateCodeActionStateRepository cl = (CreateCodeActionStateRepository) (super
					.clone());
			Vector<CreateCodeAction> clonedActions = new Vector<CreateCodeAction>();
			for (int i = 0; i < actions.size(); i++) {
				clonedActions.add((CreateCodeAction) actions.elementAt(i)
						.clone());
			}
			cl.actions = clonedActions;
			return cl;
		} catch (CloneNotSupportedException c) {
			System.out.println(c.getMessage());
		}
		return null;
	}

	public boolean add(CreateCodeAction a) {
		if (actions.contains(a) == false) {
			actions.add(a);
			return true;
		}
		return false;
	}

	public boolean remove(CreateCodeAction a) {
		if (actions.contains(a)) {
			actions.remove(a);
			return true;
		}
		return false;
	}

	public Vector<CreateCodeAction> getAllActions() {
		return actions;
	}

	public Vector<CreateCodeAction> getAllActions(SSObject a) {
		Vector<CreateCodeAction> all = new Vector<CreateCodeAction>();
		for (int i = 0; i < actions.size(); i++) {
			CreateCodeAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public Vector<CreateCodeAction> getAllActiveActions(SSObject a) {
		Vector<CreateCodeAction> all = new Vector<CreateCodeAction>();
		for (int i = 0; i < actions.size(); i++) {
			CreateCodeAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllActiveParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public Vector<CreateCodeAction> getAllInactiveActions(SSObject a) {
		Vector<CreateCodeAction> all = new Vector<CreateCodeAction>();
		for (int i = 0; i < actions.size(); i++) {
			CreateCodeAction b = actions.elementAt(i);
			Vector<SSObject> parts = b.getAllInactiveParticipants();
			for (int j = 0; j < parts.size(); j++) {
				if (parts.elementAt(j).equals(a)) {
					all.add(b);
					break;
				}
			}
		}
		return all;
	}

	public CreateCodeAction getActionWithId(int id) {
		for (int i = 0; i < actions.size(); i++) {
			CreateCodeAction act = actions.get(i);
			if (act.getId() == id) {
				return act;
			}
		}
		return null;
	}

	/*
	 * Replaces all the participants in each action with their equivalent
	 * objects in the current state. Calling this function solves the problem
	 * that happens when you clone actions -- their hashtables point to
	 * participant objects that were part of the previous, non-cloned state.
	 * Hence, this function should be called after this object is cloned.
	 */
	public void refetchParticipants(ArtifactStateRepository artifactRep,
			CustomerStateRepository customerRep,
			EmployeeStateRepository employeeRep,
			ProjectStateRepository projectRep, ToolStateRepository toolRep) {
		for (int i = 0; i < actions.size(); i++) {
			CreateCodeAction act = actions.elementAt(i);
			act.refetchParticipants(artifactRep, customerRep, employeeRep,
					projectRep, toolRep);
		}
	}
}